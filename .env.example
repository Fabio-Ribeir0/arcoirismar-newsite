# Connection string for the Postgres database (Supabase, in production).
# Use Supabase's "Session pooler" or direct connection string.
DATABASE_URL="postgresql://user:password@localhost:5432/arcoirismar?schema=public"
